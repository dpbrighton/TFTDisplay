services:
  photo-server:
    build: .
    container_name: tft-photo-server
    ports:
      - "5000:5000"
    volumes:
      - /volume1/Photos:/photos:ro
      - /volume1/docker/tft-config:/config
      - /volume1/docker/nas-server/app.py:/app/app.py:ro
    restart: unless-stopped
